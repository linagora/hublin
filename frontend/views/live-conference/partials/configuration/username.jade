.row
  .col-xs-12
    .text-center
      label #{__('Set your name in this room')}
      form.form-inline.conference-form(name='usernameForm')
        .form-group
          input.form-control(type='text', trim='true', ng-model='configuration.displayName', name='displayname', id='displayname', placeholder="#{__('Your Name')}", required)
          button.btn.btn-lg(ng-disabled="!usernameForm.$valid", type='submit' ng-click='wizard.nextStep()') #{__('Next')}

        br

        bitrate-configuration